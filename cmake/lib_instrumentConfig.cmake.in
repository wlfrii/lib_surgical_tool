get_filename_component(lib_instrument_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

# Capturing values from configure (optional)


# Same syntax ad find_package
find_dependency(Eigen3 REQUIRED)

# Any extra setup


# Add the targets file
if(NOT TARGET lib_instrument)
    include("${lib_instrument_CMAKE_DIR}/lib_instrumentTargets.cmake")
endif()


set(lib_instrument_LIBRARIES lib_instrument)